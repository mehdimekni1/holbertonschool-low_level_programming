#include "main.h"
/**
 * add- a function that adds two integers and returns the result.
 *
 * Return:0
 */
int add(int, int)
{
    int a, b, c;  
    
    a = b + c 
    printf("%d + %d = %d, b, c, a")
    return (0);
}
